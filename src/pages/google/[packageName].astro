---
import { GoogleFontDetailed } from '../../GoogleSupport/GoogleFontDetailed';
import { getFontList } from '../../GoogleSupport/useGoogleFontData';
import Layout from '../../Layout/Layout.astro';
export async function getStaticPaths() {
    return getFontList().then((list) => {
        return list.map((name) => ({ params: { packageName: name } }));
    });
}
const { packageName } = Astro.params;
---

<Layout canonicalURL="https://chinese-font.netlify.app">
    <GoogleFontDetailed packageName={packageName!} client:only="solid-js" />
</Layout>
